export const environment = {
  production: true,
  BATCH_COUNT: 50,
  BACKEND_BASE_URL: 'https://global-api.snaveware.com',
};

//BACTCH_COUNT is the number of items that will be requested from the database in a single request
